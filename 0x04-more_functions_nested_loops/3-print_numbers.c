#include "main.h"

/**
 * print_numbers - prints the numbers, from 0 to 9
 *
 * @result: input
 *
 * Result: Always o
*/

void print_numbers(void)
{
	int result;

	if (result >= 48 && result <= 57)
		_putchar (result);
	_putchar ("\n");
	return (0);
}
